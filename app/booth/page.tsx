import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "The Booth — Museum of Iris",
  description: "Drop a photo, get a vintage film strip.",
};

export default function BoothPage() {
  return (
    <div className="pt-14">
      {/* The Booth — content coming next */}
    </div>
  );
}
